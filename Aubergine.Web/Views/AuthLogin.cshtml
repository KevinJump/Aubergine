@inherits Umbraco.Web.Mvc.UmbracoTemplatePage
@{
	Layout = "AuthPages.cshtml";
}

@if (TempData["returnUrl"] != null)
{
    Response.Redirect((string)TempData["returnUrl"]);
}

@Html.Action("Login", "Authentication", new { area = "AubergineAuth" })
