@try
{
    @* the little logon man - but it will only work if the Aubergine.Auth package is installed *@
    @Html.Action("Status", "Authentication", new { area = "AubergineAuth" })
}
catch
{
    <!-- no authentication package installed -->
}